# Dependencies (โฟลเดอร์ไลบรารี ห้ามเอาขึ้นเด็ดขาด)
node_modules/

# Environment Variables (ไฟล์เก็บความลับและคีย์ต่างๆ)
.env

# Database (ไฟล์ฐานข้อมูล SQLite ในเครื่อง + ไฟล์คู่ของ WAL mode)
database.sqlite
database.sqlite-wal
database.sqlite-shm

# OS generated files (ไฟล์ขยะของระบบปฏิบัติการ)
.DS_Store
Thumbs.db
dist/

# [ชั่วคราว] Domain Proof log — มี IP ห้ามขึ้น git
domain-proof.log